<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Your Event Booking Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', Arial, sans-serif;
            line-height: 1.6;
            background-color: #f9f6fe;
            color: #2c3e50;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .email-container {
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(103, 58, 183, 0.1);
            max-width: 500px;
            width: 100%;
            overflow: hidden;
            border: 1px solid rgba(103, 58, 183, 0.2);
        }
        .email-header {
            background-color: #673AB7;
            color: white;
            text-align: center;
            padding: 20px;
        }
        .email-header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .email-content {
            padding: 30px;
            text-align: center;
        }
        .logo {
            max-width: 120px;
            margin-bottom: 15px;
        }
        .reset-code {
            background-color: #f3f0fc;
            border: 2px dashed #673AB7;
            color: #673AB7;
            padding: 20px;
            margin: 25px 0;
            font-size: 26px;
            font-weight: 600;
            letter-spacing: 8px;
            border-radius: 8px;
            display: inline-block;
        }
        .email-footer {
            background-color: #f4f1f9;
            padding: 15px;
            text-align: center;
            font-size: 12px;
            color: #8e77aa;
        }
        .user-greeting {
            color: #673AB7;
            font-weight: 600;
        }
        .reset-instructions {
            color: #666;
            margin-bottom: 25px;
        }
        @media (max-width: 600px) {
            .email-container {
                width: 95%;
                margin: 10px;
            }
            .reset-code {
                font-size: 20px;
                letter-spacing: 4px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header">
            <svg class="logo" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 40" width="120" height="40">
                <rect width="120" height="40" fill="#673AB7"/>
                <text x="60" y="25" text-anchor="middle" font-family="Arial, sans-serif" font-size="18" font-weight="bold" fill="white">Event Booking</text>
            </svg>
            <h1>Password Reset</h1>
        </div>
        
        <div class="email-content">
            <p class="user-greeting">Hello <%= name %>,</p>
            
            <p class="reset-instructions">
                We received a request to reset your password for Event Booking.
                To reset your password, please use the verification code below:
            </p>
            
            <div class="reset-code">
                <%= activationCode %>
            </div>
            
            <p class="reset-instructions">
                This code will expire in 30 minutes.
                If you didn't request this password reset, please ignore this email or contact our support team.
            </p>
        </div>
        
        <div class="email-footer">
            © <%= new Date().getFullYear() %> Event Booking | Your Event Solution<br>
            Questions? Contact support@eventbooking.com
        </div>
    </div>
</body>
</html>